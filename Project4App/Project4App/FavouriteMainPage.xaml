<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project4App.FavouriteMainPage"
             Title="Favorieten">
    
    <ListView x:Name="FavouriteView" GroupDisplayBinding="{Binding Key}" ItemsSource="{Binding Items}" IsGroupingEnabled="True">
        <ListView.GroupHeaderTemplate>
            <DataTemplate>
                <ViewCell Height="25">
                    <StackLayout>
                        <Label Text="{Binding Key}" VerticalOptions="CenterAndExpand" FontSize="Medium" FontAttributes="Bold"></Label>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
            
        </ListView.GroupHeaderTemplate>
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Text}"></TextCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>